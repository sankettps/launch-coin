<%= form_with(model: comment, local: true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%#= form.label :launcher_id %>
    <%= form.hidden_field :launcher_id, id: :comment_launcher_id %>
    <%= form.hidden_field :coin_ico_id, id: :comment_coin_ico_id %>
  </div>

  <!-- <div class="field">
    <%#= form.label :description %>
    <%#= form.text_area :description, id: :comment_description %>
  </div>

  <div class="actions">
    <%#= form.submit %>
  </div> -->
  <div class="form-group">
    <%= form.text_area :description, id: :comment_description,placeholder: "Add your comments" %>
    <!-- <textarea placeholder="Add your comments"></textarea> -->
  </div>
  <button type="submit" class="custom-btn">Post</button>
<% end %>
